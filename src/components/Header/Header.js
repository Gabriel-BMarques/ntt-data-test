import React from 'react';
import './Header.scss';  // Importa o CSS para estilização

function Header() {
    return (
        <header className="header">
            <h1 className="header-title">NERDFLIX</h1>
        </header>
    );
}

export default Header;
